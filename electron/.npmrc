# Local npm config for the Electron sub-package
# Disables strict SSL verification to handle corporate/proxy certificate chains
# (Node.js 24 changed to use its own bundled CA store which may not include local certs)
strict-ssl=false
